# CustomQueue with Generic class
## Generic
Generic classes are classes which take a type as a parameter. They are particularly useful for collection classes.
### Operation
- perform Enqueue for insertion 
- perform Dequeue for deletion
